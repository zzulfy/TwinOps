## 1. 数据与映射基线（右侧仿真专用）

- [x] 1.1 基于 `frontend/example/img1`、`img2` 与当前场景模型，产出“可交互设备对象清单”。
- [x] 1.2 建立设备对象清单与数据库 `device_code` 的映射表，并标记装饰对象（非设备）。
- [x] 1.3 编写一致性校验逻辑：检测对象集合与数据库设备集合是否 1:1 对齐。

## 2. 后端数据对齐与服务化

- [x] 2.1 扩展后端设备服务，提供右侧仿真所需的设备状态与详情数据（仅数据，不包含 UI 颜色）。
- [x] 2.2 实现数据库对齐脚本/种子生成逻辑：不一致时优先补齐或删除数据库设备，最终达到 1:1。
- [x] 2.3 增加后端单元测试：覆盖映射、状态读取、一致性校验边界。
- [x] 2.4 增加后端集成测试（localhost 接口链路）：验证设备列表数量、编码集合、详情查询一致。

## 3. 前端右侧仿真画面重构

- [x] 3.1 仅重构 Dashboard 右侧仿真区布局与视觉，不改左侧业务模块。
- [x] 3.2 移除未点击状态下的对话框与无意义提示元素。
- [x] 3.3 实现“点击设备本体 -> 页面中央设备信息对话框”主交互。
- [x] 3.4 确保所有可交互设备对象点击均可触发会话窗；装饰对象不参与点击。
- [x] 3.5 状态渲染改为透明红/透明黄/透明绿，且在非黑色背景上文本保持黑色可读。
- [x] 3.6 增加前端单元测试：覆盖点击命中、弹窗开关、状态渲染分支。

## 4. 回归与文档

- [x] 4.1 执行回归测试：Dashboard、Devices、Analysis 关键流程无回归。
- [x] 4.2 执行端到端检查：数据 1:1 对齐后，前端展示设备数量与数据库严格一致。
- [x] 4.3 更新 `README.md`：记录本变更的交互规范、1:1 一致性规则、测试命令与结果。

## 完成说明（2026-04-11）

- 2.4 已完成：新增 `DeviceLocalhostIntegrationTest`，覆盖 `GET /api/devices` 数量=51、编码集合 `DEV001`~`DEV051`、以及列表与详情一致性校验；在容器内以 localhost 链路执行通过。
- 3.6 已完成：`frontend/scripts/tests/test-centered-dialog-contract.mjs` 已修复脆弱断言并执行通过。
- 4.1 已完成：后端回归（Dashboard/Devices/Analysis）通过；前端 smoke 回归（shell readiness、analysis trigger、analysis auto refresh、device alarm two-status、auth expiry）通过。

## 端口清理记录

- 集成测试临时端口 `18080`：测试后容器进程已关闭，`netstat` 无监听。
- 前端回归临时端口 `8090`：测试后 dev server 已关闭，`netstat` 无监听。
