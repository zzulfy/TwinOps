## 1. 滚动链路重构

- [x] 1.1 将预警列表滚动位移动画从 `top` 改为 `transform`，降低布局重排成本
- [x] 1.2 增加滚动状态锁（如 `isAnimating`）防止动画重入与重叠触发
- [x] 1.3 调整轮转时机为“动画完成后再轮转数据”，避免中途内容跳变

## 2. 样式与渲染性能优化

- [x] 2.1 将预警列表高度与面板可视区对齐，减少无效绘制面积
- [x] 2.2 收敛 `.item` 过渡属性，替换 `transition: all` 为必要属性过渡
- [x] 2.3 复核 hover/阴影等样式对滚动帧率影响并做轻量化处理

## 3. 回归验证与文档同步

- [x] 3.1 补充/更新预警滚动回归脚本，覆盖平滑度、轮转正确性与无抖动场景
- [x] 3.2 执行 frontend `npm run type-check` 与 `npm run build` 并记录结果
- [x] 3.3 更新 README/前端文档中的预警滚动性能约束与验证方式
