## 1. Device Binding Contract

- [x] 1.1 扩展后端设备 DTO/API，向前端显式提供 `labelKey`，用于仿真对象绑定。
- [x] 1.2 在设备服务层增加 `labelKey` 空值/重复值防护与告警日志。
- [x] 1.3 补充契约兼容说明，确保现有设备页与分析页不受回归影响。

## 2. Dashboard 仿真交互

- [x] 2.1 在三维场景中识别并索引 51 台有效设备对象（过滤管道与辅助对象）。
- [x] 2.2 为每台设备渲染常驻标签（`deviceCode + name`），并随相机视角更新锚点位置。
- [x] 2.3 实现设备点击命中逻辑，点击后展示悬浮式设备对话框（NPC 风格）。
- [x] 2.4 对话框内容接入真实设备数据（状态、位置、遥测、告警），并支持点击空白关闭。
- [x] 2.5 对映射失败设备提供可见状态提示，避免用户误读。

## 3. 仿真-数据库一致性治理

- [x] 3.1 实现仿真设备集合与数据库设备集合差异计算。
- [x] 3.2 按策略执行修复：先删除数据库多余设备及关联数据，再补齐缺失设备及基础数据。
- [x] 3.3 将修复结果结构化输出（删除数、补齐数、失败原因、最终总数）。
- [x] 3.4 若修复失败，前端展示阻断式提示并停止错误映射渲染。

## 4. 数据与脚本保障

- [x] 4.1 补齐或恢复数据集驱动种子生成脚本，确保设备映射与 SQL 可复现。
- [x] 4.2 更新 `backend/sql/005_verify_retention.sql` 或等效校验脚本，覆盖 51 设备完整性与孤儿数据检查。
- [x] 4.3 校验仿真设备集合与 `002_seed_devices.sql` 的一一对应关系（含去除管道对象规则）。

## 5. Testing and Validation

- [x] 5.1 单元测试：模型节点过滤、`labelKey` 映射、差异计算、修复顺序（删除优先）。
- [x] 5.2 集成测试：`/api/devices` 映射字段返回、一致性修复流程、修复后设备总量与孤儿数据校验。
- [x] 5.3 回归测试：Dashboard 标签显示、设备点击弹框、Devices/Analysis 页面主流程不回归。
- [x] 5.4 在验证记录中输出单元测试、集成测试、回归测试命令与结果摘要。

## 6. Documentation

- [x] 6.1 更新 README：补充仿真设备标签与点击交互说明。
- [x] 6.2 更新 README：补充仿真-数据库 1:1 一致性校验/修复策略与排障步骤。

> 验证备注：本次已执行单元/集成/回归测试命令。集成与前端回归在当前 Windows 环境受 `Unrecognized Windows Sockets error: 10106: socket` 影响出现失败，详见 README 验证记录。
