## 1. 后端数据合同收敛

- [x] 1.1 盘点仿真页使用的后端设备字段，区分“业务数据字段”与“UI 语义字段”。
- [x] 1.2 调整后端设备 DTO/接口，仅保留业务数据字段并移除 UI 布局语义字段。
- [x] 1.3 更新后端 service/controller 及对应单元测试断言，确保接口输出与新合同一致。

## 2. 前端仿真 UI 配置前置

- [x] 2.1 新增前端仿真设备 UI 配置模块，定义对象标识、固定标签、interactive 标记和 `deviceCode` 绑定。
- [x] 2.2 重构 Dashboard 右侧仿真渲染逻辑，改为“前端 UI 配置 + 后端实时数据”合并驱动。
- [x] 2.3 保持“点击设备本体 -> 页面中央设备信息对话框”交互链路不变，移除对后端 UI 字段依赖。

## 3. 1:1 一致性与异常可观测

- [x] 3.1 实现前端集合对齐校验：比较配置设备编码集合与后端设备编码集合。
- [x] 3.2 当出现 config-only 或 data-only 设备时，输出结构化诊断信息并采用受控降级策略。
- [x] 3.3 为一致性校验与点击映射新增前端契约测试/回归 smoke 用例。

## 4. 全量验证与文档

- [x] 4.1 执行后端单元测试、集成测试（localhost）和回归测试，确认接口合同收敛无回归。
- [x] 4.2 执行前端单元测试、仿真交互 smoke 与回归测试，确认点击与渲染链路稳定。
- [x] 4.3 更新 `README.md`，记录“前端持有 UI、后端仅提供数据”的职责边界与验证结果。
