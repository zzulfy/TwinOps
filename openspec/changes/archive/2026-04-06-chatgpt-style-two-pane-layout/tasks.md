## 1. AppShell 布局基线

- [x] 1.1 新增全局 `AppShell` 组件，提供左侧导航容器与右侧内容容器。
- [x] 1.2 在路由渲染层接入 `AppShell`，确保 `/`、`/devices`、`/analysis` 统一进入 two-pane 壳层。
- [x] 1.3 定义左列窄宽约束与右列主区域占比（含桌面端最小/最大宽度边界）。

## 2. 导航与交互统一

- [x] 2.1 将模块入口标签统一迁移到左侧导航，并建立路由映射（Dashboard/Devices/Analysis）。
- [x] 2.2 实现导航选中态、悬停态、键盘焦点态，保证可访问性与状态一致性。
- [x] 2.3 移除右侧内容区域中的重复主模块入口按钮，避免入口重复。

## 3. 页面接入与样式迁移

- [x] 3.1 将 Analysis 页面接入 `AppShell`，保持现有分析列表/详情与触发流程行为不变。
- [x] 3.2 将 Devices 页面接入 `AppShell`，保持筛选、关注、告警操作行为不变。
- [x] 3.3 将 Dashboard 页面接入 `AppShell`，保持现有 3D scene 与信息面板功能不变。
- [x] 3.4 在 `app.scss` 增加 `appshell-*` 命名空间样式，避免与旧样式冲突。

## 4. 响应式与验收

- [x] 4.1 补充窄屏退化策略（左栏折叠或抽屉化）并验证交互可用性。
- [x] 4.2 更新 `frontend/README.md` 中布局结构说明与导航规范。
- [x] 4.3 执行并通过前端 type-check/build/smoke 与 backend 回归测试命令。
