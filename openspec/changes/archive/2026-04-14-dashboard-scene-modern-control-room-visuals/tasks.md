## 1. 场景空间与构图重做

- [x] 1.1 重构 `ROOM_LAYOUT`，把当前整块走廊地板改为“中轴窄通道 + 左右设备平台”。
- [x] 1.2 重构 `buildScenePlacements`，将 32 台设备整理为入口前排、左右中段、左右后段和尾端收口的分层柜列。
- [x] 1.3 调整默认镜头、intro 动画和室内约束，使开场直接聚焦设备而不是空地面。

## 2. 设备视觉层次增强

- [x] 2.1 按 `visualFamily` 强化柜体轮廓、抽屉/门板/格栅/仪表等差异，避免“同模换色”。
- [x] 2.2 提升近景主柜和中景连续柜列的层次差异，确保近中远节奏清晰。
- [x] 2.3 保持状态表达局部化，只增强小灯、边条和局部面板，不回退到整柜大面积染色。

## 3. 场景 UI 与容器风格统一

- [x] 3.1 重构右侧仿真容器内部样式，使标题板和场景叠层融入 modern control room 视觉语言。
- [x] 3.2 校准 `main-right` / `scene-canvas` / `scene-title-board` 样式，减少后台卡片感。

## 4. 验证与文档

- [ ] 4.1 本机执行前端构建与页面视觉验收，确认四个主要方向都能看到设备且默认视图不再由地板主导。
- [x] 4.2 验证前后端设备数量仍保持 1:1（当前应为 32:32）。
- [x] 4.3 更新 `README.md` 和 `frontend/README.md`，记录新的场景布局原则和视觉目标。
