## 1. 设备列表渲染结构调整

- [x] 1.1 修改设备列表项标题渲染为 `deviceCode + 空格 + 设备名称`。
- [x] 1.2 移除列表次行中的 `deviceCode · status` 拼接文本，仅保留状态标签行。
- [x] 1.3 保持设备列表筛选、选中态、关注按钮与跳转行为不变并完成回归自检。

## 2. 状态标签样式与文档同步

- [x] 2.1 在 `app.scss` 中新增/调整设备状态标签样式，覆盖 `normal / warning / error` 三种视觉状态。
- [x] 2.2 确认新样式与当前 GitHub Light 主题 token 一致，不引入硬编码色值偏离。
- [x] 2.3 更新 `frontend/README.md` 设备列表展示说明，写明标题与状态标签的新规则。

